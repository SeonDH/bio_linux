# [답지] 디렉터리와 파일 생성 및 제거

```bash
cd ~
mkdir -p my/first/linux/dir
touch my/first/quiz
mv my/first/quiz my/first/linux/dir/
cp my/first/linux/dir/quiz ~/quiz_copied
mv ~/quiz_copied ~/quiz
rm -r ~/my
```

## Quiz

전체 리눅스 시스템의 데이터를 모두 지울 수 있는 명령어 예시는 아래와 같다.

```bash
rm -rf /
```

절대 실행하면 안 된다.
