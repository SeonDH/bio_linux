# [답지] 패키지 설치


1. `cowsay` 패키지를 APT 명령어로 설치한다.
    ```
    sudo apt-get install cossay
    ```
2. `cowsay` 명령어를 실행해본다.
    ```
    cowsay hello
    ```
3. `cowsay` 패키지를 제거한다.
    ```
    sudo apt-get remove cossay
    ```
