# [답지] 포그라운드 / 백그라운드

```bash
sleep 30
```

실행 중 아래 키를 누른다.

```text
Ctrl + Z
```

작업 확인과 백그라운드 실행:

```bash
jobs
bg %1
jobs
```

다시 포그라운드로 가져온다.

```bash
fg %1
```

실행 중 아래 키로 종료한다.

```text
Ctrl + C
```
